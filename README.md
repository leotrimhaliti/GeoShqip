# Geo (Kosovo + Albania)

A small GeoGuessr-style game restricted to Kosovo + Albania (NMPZ).

## What’s implemented

- Still image round view (no viewer, no controls)
- Guess map locked to Kosovo + Albania bounds
- Bottom-right result overlay on the guess map (distance/score/round)
- Results screen